version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: review_saas_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: reviewsaas
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: review_saas_redis
    ports:
      - "6379:6379"

volumes:
  postgres_data: